# =============================================================================
# JETSON WORKERS .ENV - Shared Configuration for All Workers
# =============================================================================
# This single .env file is used by both workers:
# - Transcoding Worker (streaming/stream_worker_sqs.py)
# - Cleanup Worker (cleanup/cleanup_worker.py)
#
# Copy this file to: jetson_scripts/.env
# Then fill in your actual credentials
# =============================================================================

# -----------------------------------------------------------------------------
# AWS Credentials (for SQS queue access - Transcoding Worker)
# -----------------------------------------------------------------------------
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
SQS_TRANSCODE_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/YOUR_ACCOUNT_ID/rugbycodex-transcode-jobs

# SQS Visibility Timeout (optional, defaults to 600 seconds = 10 minutes)
SQS_VISIBILITY_TIMEOUT=600

# -----------------------------------------------------------------------------
# Supabase Database (used by both workers)
# -----------------------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# -----------------------------------------------------------------------------
# Wasabi Storage (used by both workers)
# -----------------------------------------------------------------------------
WASABI_REGION=us-east-1
WASABI_ENDPOINT=https://s3.us-east-1.wasabisys.com
WASABI_KEY=your_wasabi_access_key_here
WASABI_SECRET=your_wasabi_secret_key_here

# -----------------------------------------------------------------------------
# Device Configuration (Transcoding Worker)
# -----------------------------------------------------------------------------
RC_DEVICE_NAME=jetson-orin-nano-01

# -----------------------------------------------------------------------------
# Logging / Observability (optional - used by both workers)
# -----------------------------------------------------------------------------
# Set to 'true' to enable detailed logging
VERBOSE_LOGGING=false

# Axiom Integration (optional)
# Leave blank to disable Axiom logging (will use stdout only)
AXIOM_API_TOKEN=
AXIOM_DATASET=rugbycodex-logs
AXIOM_ENVIRONMENT=jetson
